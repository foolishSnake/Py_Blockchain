import json
import Account from Account

class AccountManager:
    accounts = []
    last_id = Nine

    def add_count(self, name):

        return

    def added_transaction(self, from_acc, to_acc, amount, note, block_hash ):

        return
